import { PerformenceValeurC } from './performence-valeur-c';

describe('PerformenceValeurC', () => {
  it('should create an instance', () => {
    expect(new PerformenceValeurC()).toBeTruthy();
  });
});
