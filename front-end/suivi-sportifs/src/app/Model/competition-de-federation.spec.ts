import { CompetitionDeFederation } from './competition-de-federation';

describe('CompetitionDeFederation', () => {
  it('should create an instance', () => {
    expect(new CompetitionDeFederation()).toBeTruthy();
  });
});
